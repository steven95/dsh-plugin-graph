# dsh-plugin-graph bundle 声明
# name 必须是可导入的包名（npm 包名），loader 按它 import；插件内部 export const name 才是插件名
# inject 声明插件依赖的 cordis 服务（tools），loader 据此在 apply 前注入
- insert:
    - id: plugin-graph
      name: dsh-plugin-graph-engine
      inject: [tools]
